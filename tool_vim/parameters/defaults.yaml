# vim: ft=yaml
#
# Set default values.
---
values:
  lookup:
    pkg:
      name: vim
    paths:
      confdir: ''
      conffile: '.vimrc'
      xdg_dirname: 'vim'
      xdg_conffile: 'vimrc'
    rootgroup: root
  # Just here for testing
  added_in_defaults: defaults_value
  winner: defaults
...
